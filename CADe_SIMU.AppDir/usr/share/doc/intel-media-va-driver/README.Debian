Using the iHD VAAPI driver
==========================

Currently the drivers is not tried by libva automatically. One needs to manually
force the use of the driver by setting LIBVA_DRIVER_NAME:

  export LIBVA_DRIVER_NAME=iHD
